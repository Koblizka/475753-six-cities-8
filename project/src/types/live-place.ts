export type LivePlaceProps = {
  id: string,
  isPremium: boolean,
  isBookmarked: boolean,
  placeType: string,
  price: number,
  rating: number,
  name: string,
  photoSrc: string,
  city: string,
}
